# Test profile for FlywayMigrateIT. JDBC URL/credentials come from
# @ServiceConnection (Testcontainers dynamic mapping) — no hardcoded secrets.
# Env overrides still honoured when present.
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.open-in-view=false
